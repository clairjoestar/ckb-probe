[workspace]
members = [
    "ckb-probe",
    "ckb-probe-common",
    "xtask",
]
exclude = [
    "ckb-probe-ebpf",  # Built separately: cargo +nightly build --target bpfel-unknown-none -Z build-std=core
]
resolver = "2"

[workspace.package]
version = "0.1.1"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/xxx/ckb-probe"
